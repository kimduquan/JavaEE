hazelcast:
  yaml:
    hazelcast:
      network:
        join:
          kubernetes:
            service-dns: "${serviceName}.${namespace}.svc.cluster.local"
            service-name: ''
            namespace: ''
        rest-api:
          enabled: true
rbac:
  create: false
cluster:
  memberCount: 1
mancenter:
  enabled: false